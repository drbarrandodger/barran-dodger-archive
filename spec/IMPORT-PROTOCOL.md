# Import protocol

## Rules for later ingestion work

- keep original filename and original repository path
- record import timestamp separately from source timestamp
- create derived metadata rather than rewriting the source artefact
- flag sensitivity and publication status independently from preservation status
- retain the originating collection root (`attached_assets`, `client/public/documents`, and so on)
